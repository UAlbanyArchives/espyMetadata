<%= form_with(model: big_card, local: true) do |form| %>

  <div class="col-lg-12 mx-auto ocrFix">
    <%= form.text_area :ocr_text, id: :big_card_ocr_text, rows: "15" %>
  </div>

  <div class="col-md-12 center">
  
    <%= form.button '<i class="fa fa-check"></i> Update Index Card'.html_safe, class: "btn btn-success" %>
  </div>

<% end %>
